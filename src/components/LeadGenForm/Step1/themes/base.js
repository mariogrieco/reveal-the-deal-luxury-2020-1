// @flow
// import car from '../images/cars/base.png'

export default function (globalTheme) {
    return {
        // car,
        background: globalTheme.colors.primary,
        color: '#FFFFFF',
        submit: { background: globalTheme.colors.primary, color:'#FFFFFF', marginTop: '1.62em' }
    }
}
