// @flow
import background from '../images/voucher_value_bg/base.png'

export default function (globalTheme) {
    return {
        bgColor: '#000000',
        color: globalTheme.colors.primary,
        titleColor: '#FFFFFF',
        background
    }
}
