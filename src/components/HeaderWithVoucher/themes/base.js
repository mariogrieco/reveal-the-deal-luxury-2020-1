// @flow

export default function (globalTheme) {
    return {
        cars: null,
        mask: null,
        colors: globalTheme.colors,
        background: null,
        brandLogoInverted: null,
        primaryColor: globalTheme.colors.primary,
        inputColor: globalTheme.colors.ternary,
        buttonBg: globalTheme.colors.secondary,
        buttonColor: '#fff'
    }
}
